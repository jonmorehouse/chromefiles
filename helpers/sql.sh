# join two tables using mysql
# select property_name.property_id, property_name.name from property_name left join (status) on property_name.property_id = status.property_id where status.property_status=true into outfile "~/properties.txt"
